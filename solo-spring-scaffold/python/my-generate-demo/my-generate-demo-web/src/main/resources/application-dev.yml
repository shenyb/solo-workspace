# ─── 开发环境（H2 内存库，开箱即用） ───
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:my-generate-demo
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console
